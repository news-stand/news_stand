language: node_js
node_js:
  - "8.4.0"
beofre_install:
  - mkdir -p ~/shared
jobs:
  include:
    - stage: compile
      script: babel ./app/server -d ~/shared/build/server 
    - stage: test
      script: node ~/shared/build/server/index.js & npm run test