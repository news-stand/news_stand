{
  "name": "news_stand",
  "scripts": {
  },
  "env": {
    "NEWS_KEY": {
      "description": "API key for newsAPI"
    },
    "CLIENT_ID": {
      "description": "client ID for google+ OAuth",
      "required": true
    },
    "CLIENT_SECRET": {
      "description": "client secret for google+ Auth.",
      "required": true
    },
    "MONGODB_URI": {
      "description": "URI for mlab DB",
      "required": true
    },
    "COOKIE_KEY": {
      "description": "key for encrypting cookies",
      "required": true
    }
  },
  "formation": {
  },
  "addons": [

  ],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ]
}
